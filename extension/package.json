{
  "name": "@meritio/extension",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "zip": "rm -f meritio-extension.zip && cd dist && zip -qr ../meritio-extension.zip . && cd .. && ls -la meritio-extension.zip",
    "release": "npm run build && npm run zip && bash ../scripts/publish-extension.sh && git -C ../../meritio-extension push origin main"
  },
  "dependencies": {
    "@meritio/shared": "*",
    "jszip": "^3.10.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "superdoc": "^2.17.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.270",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  },
  "license": "AGPL-3.0-only"
}
